# spring ???
spring:

  # ?? ??? ?? ( ??? ????? ??? ?? )
  time-zone: Asia/Seoul

  # DB ??
  datasource:
    url: jdbc:mysql://localhost:3306/jangMarket
    username: root
    password: kkhj7346
    driver-class-name: com.mysql.cj.jdbc.Driver

  # jpa ??
  jpa:
    hibernate:
      ddl-auto: create # ??? ?? ??? update ? ?? ??
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    security:
      enable-csrf: false
base-url: "http://localhost:8080"
redirect-url: "http://localhost:8080/swagger-ui/index.html"

server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jmarket
    username: root
    password: Yeohuiju03 #${DB_PASSWORD}

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

  web:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

  time-zone: Asia/Seoul

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
    security:
      enable-csrf: false
  jwt:
    secret: "hadgfsfsfsfsssfsfssfsfsfsffasadad1234567890"
    expiration:
      access-token: 3600
      refresh-token: 604800
  security:
    oauth2:
      client:
        kakao-path-uri: "/authorization/oauth2/code/kakao"
        registration:
          kakao:
            client-id: 28aafe4adf59867d844dce294b455c6c
            client-secret: JuOWB45mg985VegMqx7bmthrXNEb9l6O
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/authorization/oauth2/code/kakao
            scope:
              - account_email
              - profile_nickname
            client-name: Kakao
            client-authentication-method: client_secret_post
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

cloud:
  aws:
    s3:
      bucket: ${AWS_BUKET}
    credentials:
      access-key: ${AWS_ACCESS_KEY_ID}
      secret-key: ${AWS_SECRET_ACCESS_KEY}
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false